# Database Connection
DATABASE_URL=postgresql+asyncpg://user:password@host:port/database

# Service Authentication (for service-to-service calls if needed)
SERVICE_AUTH_TOKEN=your_secret_token_here

# Application Settings
ENV=development
LOG_LEVEL=INFO
